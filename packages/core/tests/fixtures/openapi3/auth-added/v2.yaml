openapi: 3.0.0
info:
  title: Test API
  version: 2.0.0
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
paths:
  /users:
    get:
      summary: Get users
      security:
        - bearerAuth: []
      responses:
        '200':
          description: OK
